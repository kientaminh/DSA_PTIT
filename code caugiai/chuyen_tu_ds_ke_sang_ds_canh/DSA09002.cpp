#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    int n;
    cin >> n;
    cin.ignore();
    for(int u = 1; u <= n; u++)
    {
        string s;
        getline(cin, s);
        stringstream ss(s);
        while(ss >> s)
        {

        }
    }


    return 0;
}
