#include <bits/stdc++.h>

using namespace std;

const int mod = 1e9 + 7;

long long tinh(int n, int k)
{
    long long res = 1;
    while(k)
}

int main() {
    ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    int t;
    cin >> t;
    while(t--)
    {
        int n, k;
        cin >> n >> k;

    }


    return 0;
}
